/**
 * Hashes a string of data using SHA-256.
 *
 * @param {any} input - the string to hash.
 *
 * @return {Uint8Array} the hash digest.
 */
export declare function sha256digest(input: any): Uint8Array;
